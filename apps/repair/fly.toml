# fly.toml — deploy from apps/repair
# Working directory: apps/repair
# Config path: apps/repair/fly.toml
# Internal port: 3001

app = "sd-solutions-repair"
primary_region = "arn"

[build]
  dockerfile = "Dockerfile"

[deploy]
  # Avoid shell scripts (Windows CRLF → exit 127 "not found" on Linux).
  # NODE_PATH is set in the image for migrate-node packages.
  release_command = "node /app/scripts/fly-db-migrate.cjs"

[env]
  PORT = "3001"
  NODE_ENV = "production"
  CI = "true"

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

[mounts]
  source = "repair_uploads"
  destination = "/app/public/uploads"
